import CSSInterface from 'react-with-styles-interface-css';

import registerInterfaceWithBootstrap4Theme from './registerInterfaceWithBootstrap4Theme';

export default function registerCSSInterfaceWithBootstrap4Theme() {
  registerInterfaceWithBootstrap4Theme(CSSInterface);
}
